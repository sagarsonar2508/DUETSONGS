# Custom character art

Sanitized sprites installed by the local admin panel
(http://127.0.0.1:5173/admin.html in dev). Naming: `<charId>.<pose>.png`
(e.g. `cat-m.idle.png`, `cat-m.sing.png`). Characters without files here
use the built-in sculpted SVG art. See ../../../ARTWORK_GUIDE.md.
