# Song posters

Optional square PNGs installed by the admin studio (Songs tab):
`<songId>.png` (512×512, center-cropped). Songs without a poster keep
their gradient card. Song ids: see app/src/data/songs.ts.
