/**
 * Entry for the local Character Studio (admin.html, dev-server only —
 * `vite build` bundles index.html alone, so none of this ships).
 * The studio lives in src/admin/.
 */

import './admin/main';
